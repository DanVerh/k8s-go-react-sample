name: DB Image CI

on:
  push:
    branches: [ "main" ]
    paths:
      - "db/**"
      - ".github/workflows/db-ci.yml"
  pull_request:
    branches: [ "main" ]
    

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Docker Build
      working-directory: ./db
      run: docker image build --tag danverh/db-sample:latest .
    
    - name: Docker Login
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Docker Push
      working-directory: ./db
      run: docker image push danverh/db-sample:latest
